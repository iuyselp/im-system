# IM 通信系统 - V1.5 开发计划

## 版本目标
在 V1.0 MVP 基础上，增加音视频通话、消息已读回执、聊天记录搜索等核心功能。

## 功能清单

### 1. 消息已读回执 ✅
- [x] 数据库字段扩展（im_message_read 表）
- [x] 标记已读接口
- [x] 获取未读消息数接口
- [x] 已读状态推送
- [x] 前端集成（Chat.vue）

### 2. 聊天记录搜索 ✅
- [x] 消息搜索接口（按关键词、时间范围、发送者）
- [x] 搜索结果高亮
- [x] 前端搜索组件（MessageSearch.vue）
- [x] 集成到聊天界面

### 3. 音视频通话 ✅
- [x] 通话记录表（im_call_record）
- [x] 信令服务（呼叫、接听、挂断）
- [x] WebRTC 配置接口（STUN/TURN）
- [x] 通话历史查询
- [x] 前端通话组件（CallModal.vue）
- [x] 通话状态管理（store/call.js）
- [x] 通话历史页面（CallHistory.vue）
- [x] 路由配置

## 技术实现

### 后端新增
- ✅ `CallRecord.java` - 通话记录实体
- ✅ `CallService.java` - 通话服务
- ✅ `CallController.java` - 通话接口
- ✅ `MessageReadStatus.java` - 消息已读状态实体
- ✅ `MessageReadService.java` - 已读服务
- ✅ `MessageReadController.java` - 已读接口

### 前端新增
- ✅ `views/Chat.vue` - 更新集成通话和搜索功能
- ✅ `views/CallHistory.vue` - 通话历史页面
- ✅ `components/CallModal.vue` - 通话弹窗组件
- ✅ `components/MessageSearch.vue` - 搜索组件
- ✅ `api/call.js` - 通话 API
- ✅ `api/messageRead.js` - 已读 API
- ✅ `store/call.js` - 通话状态管理

### 数据库变更
```sql
-- 消息已读状态表
CREATE TABLE `im_message_read` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `message_id` varchar(64) NOT NULL,
  `user_id` bigint NOT NULL,
  `read_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_msg_user` (`message_id`, `user_id`),
  KEY `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 通话记录表
CREATE TABLE `im_call_record` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `call_id` varchar(64) NOT NULL,
  `caller_id` bigint NOT NULL,
  `receiver_id` bigint DEFAULT NULL,
  `group_id` bigint DEFAULT NULL,
  `type` varchar(20) NOT NULL COMMENT 'AUDIO/VIDEO',
  `duration` int DEFAULT 0,
  `status` varchar(20) DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_caller` (`caller_id`),
  KEY `idx_receiver` (`receiver_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 开发时间线
- ✅ Day 1: 数据库设计 + 消息已读功能
- ✅ Day 2: 聊天记录搜索功能
- ✅ Day 3-4: 音视频通话信令服务
- ✅ Day 5: 前端集成 + 测试

## 待完成事项

### 高优先级
- [ ] WebSocket 实时信令推送（呼叫提醒、状态同步）
- [ ] WebRTC 媒体流实际连接
- [ ] 图片/文件上传功能
- [ ] 群聊功能完善

### 中优先级
- [ ] 消息类型支持（图片、语音、文件）
- [ ] 消息撤回/删除 UI 交互
- [ ] 联系人列表管理
- [ ] 群组管理界面

### 低优先级
- [ ] 消息转发功能
- [ ] 表情包支持
- [ ] 主题切换
- [ ] 离线推送

---
*创建日期：2026-03-14*
*最后更新：2026-03-14 前端集成完成*
