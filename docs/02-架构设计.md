# IM 通信系统 - 架构设计文档

## 1. 系统架构

### 1.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              Client Layer                                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐                │
│  │   Web    │  │  Mobile  │  │ Desktop  │  │  Admin   │                │
│  │  (Vue3)  │  │  (App)   │  │  (Electron)│ │  Panel   │                │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘                │
└───────┼─────────────┼─────────────┼─────────────┼───────────────────────┘
        │             │             │             │
        └─────────────┴──────┬──────┴─────────────┘
                             │ HTTPS/WSS
┌────────────────────────────┼────────────────────────────────────────────┐
│                         Gateway Layer                                   │
│                    ┌────────┴────────┐                                  │
│                    │   Nginx / SLB   │                                  │
│                    │  (负载均衡)      │                                  │
│                    └────────┬────────┘                                  │
└─────────────────────────────┼───────────────────────────────────────────┘
                              │
┌─────────────────────────────┼───────────────────────────────────────────┐
│                      Application Layer                                  │
│  ┌──────────────────────────┴───────────────────────────────┐          │
│  │                    API Gateway                            │          │
│  │              (认证/限流/路由/日志)                          │          │
│  └──────────────────────────┬───────────────────────────────┘          │
│                             │                                           │
│  ┌────────────┐  ┌─────────┴─────────┐  ┌────────────┐                 │
│  │   User     │  │    Chat Service   │  │  Customer  │                 │
│  │  Service   │  │   (WebSocket)     │  │  Service   │                 │
│  └────────────┘  └─────────┬─────────┘  └────────────┘                 │
│                            │                                            │
│  ┌────────────┐  ┌─────────┴─────────┐  ┌────────────┐                 │
│  │   Group    │  │     Call Service  │  │   File     │                 │
│  │  Service   │  │    (WebRTC)       │  │  Service   │                 │
│  └────────────┘  └───────────────────┘  └────────────┘                 │
└─────────────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────┼───────────────────────────────────────────┐
│                       Message Queue                                     │
│                    ┌────────┴────────┐                                  │
│                    │  RabbitMQ /     │                                  │
│                    │   RocketMQ      │                                  │
│                    └────────┬────────┘                                  │
└─────────────────────────────┼───────────────────────────────────────────┘
                              │
┌─────────────────────────────┼───────────────────────────────────────────┐
│                        Data Layer                                       │
│  ┌────────────┐  ┌─────────────┐  ┌────────────┐  ┌────────────┐       │
│  │   MySQL    │  │    Redis    │  │  MinIO/OSS │  │ Elasticsearch│      │
│  │  (主从)     │  │   (集群)    │  │  (对象存储) │  │  (消息搜索)   │      │
│  └────────────┘  └─────────────┘  └────────────┘  └────────────┘       │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.2 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                      前端架构 (Vue3)                         │
├─────────────────────────────────────────────────────────────┤
│  View Layer:  Components / Pages / Layouts                  │
│  State:       Pinia (状态管理)                               │
│  Router:      Vue Router 4                                   │
│  HTTP:        Axios + Interceptors                           │
│  WebSocket:   @vueuse/core + 重连机制                        │
│  WebRTC:      simple-peer / mediasoup-client               │
│  UI:          Element Plus + 自定义主题                      │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    后端架构 (Spring Boot)                    │
├─────────────────────────────────────────────────────────────┤
│  Controller:  REST API + Swagger 文档                        │
│  Service:     业务逻辑 + 事务管理                            │
│  Mapper:      MyBatis Plus + 动态 SQL                        │
│  WebSocket:   Spring WebSocket + STOMP                      │
│  Security:    Spring Security + JWT                         │
│  Cache:       Redis + 本地缓存                               │
│  MQ:          RabbitMQ / RocketMQ                           │
│  Storage:     MinIO / OSS SDK                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 核心模块设计

### 2.1 用户模块

```
UserService
├── register()           # 用户注册
├── login()              # 用户登录
├── logout()             # 用户登出
├── refreshToken()       # 刷新 Token
├── getUserInfo()        # 获取用户信息
├── updateUserInfo()     # 更新用户信息
├── changePassword()     # 修改密码
├── getContacts()        # 获取通讯录
├── addFriend()          # 添加好友
├── handleFriendRequest()# 处理好友请求
└── blockUser()          # 拉黑用户
```

### 2.2 聊天模块

```
ChatService
├── sendMessage()        # 发送消息
├── receiveMessage()     # 接收消息 (WebSocket)
├── getMessageList()     # 获取消息列表
├── revokeMessage()      # 撤回消息
├── deleteMessage()      # 删除消息
├── forwardMessage()     # 转发消息
├── markAsRead()         # 标记已读
├── getUnreadCount()     # 获取未读数
└── syncMessages()       # 消息同步
```

### 2.3 群组模块

```
GroupService
├── createGroup()        # 创建群组
├── updateGroup()        # 更新群信息
├── dismissGroup()       # 解散群组
├── transferGroup()      # 转让群主
├── inviteMembers()      # 邀请成员
├── removeMembers()      # 移除成员
├── quitGroup()          # 退出群组
├── setAdmin()           # 设置管理员
├── muteMember()         # 禁言成员
├── getGroupInfo()       # 获取群信息
└── getMemberList()      # 获取成员列表
```

### 2.4 通话模块

```
CallService
├── startCall()          # 发起通话
├── answerCall()         # 接听通话
├── rejectCall()         # 拒绝通话
├── endCall()            # 结束通话
├── switchCamera()       # 切换摄像头
├── muteAudio()          # 静音
├── muteVideo()          # 关闭视频
├── inviteParticipant()  # 邀请参与者
└── getCallHistory()     # 通话记录
```

### 2.5 客服模块

```
CustomerService
├── createTicket()       # 创建工单
├── assignTicket()       # 分配工单
├── transferTicket()     # 转接工单
├── closeTicket()        # 关闭工单
├── getTicketList()      # 工单列表
├── sendTicketMessage()  # 发送工单消息
├── getKnowledgeList()   # 知识库列表
├── searchKnowledge()    # 搜索知识库
└── evaluateService()    # 服务评价
```

---

## 3. WebSocket 设计

### 3.1 连接管理

```java
// WebSocket 配置
@Configuration
@EnableWebSocketMessageBroker
public class WebSocketConfig implements WebSocketMessageBrokerConfigurer {
    
    // 注册 STOMP 端点
    @Override
    public void registerStompEndpoints(StompEndpointRegistry registry) {
        registry.addEndpoint("/ws")
                .setAllowedOriginPatterns("*")
                .withSockJS()
                .setHeartbeatTime(25000);
    }
    
    // 配置消息代理
    @Override
    public void configureMessageBroker(MessageBrokerRegistry registry) {
        registry.enableSimpleBroker("/topic", "/queue");
        registry.setApplicationDestinationPrefixes("/app");
        registry.setUserDestinationPrefix("/user");
    }
}
```

### 3.2 消息类型

```typescript
// 消息类型定义
export enum MessageType {
  TEXT = 'TEXT',
  IMAGE = 'IMAGE',
  VOICE = 'VOICE',
  VIDEO = 'VIDEO',
  FILE = 'FILE',
  LOCATION = 'LOCATION',
  SYSTEM = 'SYSTEM',
  CALL_INVITE = 'CALL_INVITE',
  CALL_ANSWER = 'CALL_ANSWER',
  CALL_END = 'CALL_END',
}

// 消息结构
export interface Message {
  id: string;
  type: MessageType;
  fromId: string;
  toId: string;
  groupId?: string;
  content: string;
  extra?: any;
  status: MessageStatus;
  timestamp: number;
}
```

### 3.3 断线重连

```typescript
// WebSocket 重连机制
class WebSocketManager {
  private reconnectCount = 0;
  private maxReconnect = 5;
  private reconnectDelay = 3000;
  
  connect() {
    this.ws = new WebSocket(url);
    
    this.ws.onclose = () => {
      if (this.reconnectCount < this.maxReconnect) {
        setTimeout(() => {
          this.reconnectCount++;
          this.connect();
        }, this.reconnectDelay * this.reconnectCount);
      }
    };
  }
  
  // 心跳检测
  startHeartbeat() {
    setInterval(() => {
      this.send({ type: 'PING' });
    }, 30000);
  }
}
```

---

## 4. 消息可靠性设计

### 4.1 消息投递保证

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Sender  │────▶│   MQ     │────▶│  Service │────▶│ Receiver │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
     │                │                │                │
     │  1. 发送消息    │                │                │
     │───────────────▶│                │                │
     │                │  2. 持久化      │                │
     │                │───────────────▶│                │
     │                │                │  3. 推送       │
     │                │                │───────────────▶│
     │                │                │  4. ACK        │
     │                │                │◀───────────────│
     │  5. 确认       │                │                │
     │◀───────────────│                │                │
     │                │  6. 离线存储    │                │
     │                │ (如果未 ACK)    │                │
```

### 4.2 消息去重

```java
// 消息去重
public class MessageDeduplication {
    
    @Autowired
    private RedisTemplate<String, String> redisTemplate;
    
    public boolean isDuplicate(String messageId) {
        String key = "msg:duplicate:" + messageId;
        Boolean exists = redisTemplate.opsForValue()
            .setIfAbsent(key, "1", 24, TimeUnit.HOURS);
        return !Boolean.TRUE.equals(exists);
    }
}
```

### 4.3 离线消息

```java
// 离线消息存储
@Service
public class OfflineMessageService {
    
    // 存储离线消息
    public void store(String userId, Message message) {
        String key = "offline:msg:" + userId;
        redisTemplate.opsForList().rightPush(key, toJson(message));
    }
    
    // 获取离线消息
    public List<Message> getOfflineMessages(String userId) {
        String key = "offline:msg:" + userId;
        List<String> messages = redisTemplate.opsForList().range(key, 0, -1);
        redisTemplate.delete(key); // 读取后删除
        return messages.stream().map(this::fromJson).collect(Collectors.toList());
    }
}
```

---

## 5. 数据库设计

### 5.1 核心表结构

```sql
-- 用户表
CREATE TABLE `im_user` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `username` varchar(50) NOT NULL,
  `password` varchar(100) NOT NULL,
  `nickname` varchar(50) DEFAULT NULL,
  `avatar` varchar(255) DEFAULT NULL,
  `phone` varchar(20) DEFAULT NULL,
  `email` varchar(100) DEFAULT NULL,
  `status` tinyint DEFAULT 1 COMMENT '1-正常 0-禁用',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  UNIQUE KEY `uk_phone` (`phone`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 好友关系表
CREATE TABLE `im_friend` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `friend_id` bigint NOT NULL,
  `remark` varchar(50) DEFAULT NULL,
  `status` tinyint DEFAULT 1 COMMENT '1-好友 0-拉黑',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_friend` (`user_id`, `friend_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 群组表
CREATE TABLE `im_group` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `name` varchar(100) NOT NULL,
  `avatar` varchar(255) DEFAULT NULL,
  `owner_id` bigint NOT NULL,
  `notice` text,
  `max_members` int DEFAULT 500,
  `status` tinyint DEFAULT 1,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 群成员表
CREATE TABLE `im_group_member` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `group_id` bigint NOT NULL,
  `user_id` bigint NOT NULL,
  `role` tinyint DEFAULT 0 COMMENT '0-成员 1-管理员 2-群主',
  `alias` varchar(50) DEFAULT NULL,
  `mute_until` datetime DEFAULT NULL,
  `joined_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_group_user` (`group_id`, `user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 消息表（分表）
CREATE TABLE `im_message_0` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `msg_id` varchar(64) NOT NULL,
  `from_id` bigint NOT NULL,
  `to_id` bigint,
  `group_id` bigint,
  `type` varchar(20) NOT NULL,
  `content` text NOT NULL,
  `extra` json DEFAULT NULL,
  `status` tinyint DEFAULT 1 COMMENT '1-正常 0-撤回 2-删除',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_msg_id` (`msg_id`),
  KEY `idx_from` (`from_id`),
  KEY `idx_to` (`to_id`),
  KEY `idx_group` (`group_id`),
  KEY `idx_created` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 通话记录表
CREATE TABLE `im_call_record` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `call_id` varchar(64) NOT NULL,
  `caller_id` bigint NOT NULL,
  `receiver_id` bigint,
  `group_id` bigint,
  `type` varchar(20) NOT NULL COMMENT 'AUDIO/VIDEO',
  `duration` int DEFAULT 0,
  `status` varchar(20) DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_caller` (`caller_id`),
  KEY `idx_receiver` (`receiver_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 客服工单表
CREATE TABLE `im_ticket` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `ticket_no` varchar(32) NOT NULL,
  `user_id` bigint NOT NULL,
  `agent_id` bigint,
  `title` varchar(200) NOT NULL,
  `status` varchar(20) DEFAULT 'OPEN',
  `priority` varchar(20) DEFAULT 'NORMAL',
  `rating` tinyint DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_ticket_no` (`ticket_no`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 6. API 设计

### 6.1 认证接口

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/auth/register` | POST | 用户注册 |
| `/api/auth/login` | POST | 用户登录 |
| `/api/auth/logout` | POST | 用户登出 |
| `/api/auth/refresh` | POST | 刷新 Token |

### 6.2 用户接口

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/user/info` | GET | 获取用户信息 |
| `/api/user/update` | PUT | 更新用户信息 |
| `/api/user/password` | PUT | 修改密码 |
| `/api/user/contacts` | GET | 获取通讯录 |

### 6.3 消息接口

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/message/list` | GET | 获取消息列表 |
| `/api/message/revoke` | POST | 撤回消息 |
| `/api/message/delete` | POST | 删除消息 |
| `/api/message/read` | POST | 标记已读 |

### 6.4 群组接口

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/group/create` | POST | 创建群组 |
| `/api/group/info` | GET | 获取群信息 |
| `/api/group/members` | GET | 获取成员列表 |
| `/api/group/invite` | POST | 邀请成员 |

---

## 7. 部署架构

### 7.1 开发环境

```yaml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    ports:
      - "3306:3306"
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
  minio:
    image: minio/minio
    ports:
      - "9000:9000"
  backend:
    build: ./im-backend
    ports:
      - "8080:8080"
  frontend:
    build: ./im-frontend
    ports:
      - "5173:5173"
```

### 7.2 生产环境

- Nginx 负载均衡
- 多实例后端服务
- Redis 哨兵模式
- MySQL 主从复制
- MinIO 集群

---

*文档版本：1.0*
*创建日期：2026-03-13*
